const express = require('express');
const jwt = require('jsonwebtoken');
// need to set up db first to get data from?

class Authenticate {
  static authenticateUser(){
    //find user 

    //==>find user  error

    //check pw match 

    //==> pw match error 
  }

  //if user and pw is right send token
  static generateToken(){

  }
}